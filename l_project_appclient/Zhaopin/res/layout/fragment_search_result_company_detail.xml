<?xml version="1.0" encoding="utf-8"?>
<TabHost xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@android:id/tabhost"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white" >

    <LinearLayout
        android:id="@+id/search_result_company_detail_tabs"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical" >

        <TabWidget
            android:id="@android:id/tabs"
            style="@style/common_tabWidget_style"
            android:layout_weight="0" />

        <FrameLayout
            android:id="@android:id/tabcontent"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="0" >
        </FrameLayout>

        <android.support.v4.view.ViewPager
            android:id="@+id/search_result_company_detail_pager"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />
        <Button android:id="@+id/search_result_company_detail_comment"
        style="@style/common_button_green_big"
        android:layout_gravity="center_horizontal"
        android:text="@string/exposure_search_result_detail_comment"
        android:visibility="gone"/>
    </LinearLayout>
    

    

</TabHost>