<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/adzone_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:gravity="center"
    android:padding="0dp" >

    <LinearLayout
        android:id="@+id/adzone_item_imgview_bg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:padding="0dp"
        android:orientation="vertical" >

        <ImageView
            android:id="@+id/adzone_item_imgview"
            android:layout_width="match_parent"
            android:layout_height="141dp"
            android:adjustViewBounds="true"
            android:scaleType="centerCrop"
            android:background="@drawable/qianniu_bg_small" 
            android:visibility="gone"/>
    </LinearLayout>

    <!-- 
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:layout_marginTop="10dp"
        android:padding="10dp" -->
    <TextView
        android:id="@+id/adzone_item_title"
        android:layout_width="match_parent"
        android:layout_height="27dp"
        android:layout_alignLeft="@id/adzone_item_imgview_bg"
        android:layout_below="@id/adzone_item_imgview_bg"
        android:ellipsize="end"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:gravity="center_vertical"
        android:includeFontPadding="false"
        android:singleLine="true"
        android:text="jldowoejfoewjfowejf"
        android:textColor="@color/list_subTitle"
        android:textSize="@dimen/text_size_12" />

</RelativeLayout>